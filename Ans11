#include <iostream>
#include <cmath>
using namespace std;

int main() {
	float p,r,t,SI;
	scanf("%f",&p);
	scanf("%f",&r);
	scanf("%f",&t);
	printf("The simple interest for this principal amount is ");
	printf("%f",SI=p*r*t/100);
	return 0;
}
